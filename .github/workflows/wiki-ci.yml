name: Tideborne Wiki CI

on:
  push:
    branches:
      - wiki/official-field-guide
    paths:
      - 'site/**'
      - 'docs/wiki/**'
      - '.github/workflows/wiki-ci.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install website dependencies
        working-directory: site
        run: npm install

      - name: Commit generated lockfile once
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- site/package-lock.json && git ls-files --error-unmatch site/package-lock.json >/dev/null 2>&1; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add site/package-lock.json
          git commit -m "chore(wiki): lock site dependencies"
          git push origin HEAD:wiki/official-field-guide

      - name: Typecheck and build
        working-directory: site
        run: npm run build

      - name: Verify repository base-path output
        working-directory: site
        shell: bash
        run: |
          set -euo pipefail
          test -d dist
          if grep -R -E '(href|src)="/(fish|assets)/' dist --include='*.html'; then
            echo 'Found a root-relative custom link that would break under /TIDEBORNE.' >&2
            exit 1
          fi
